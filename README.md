# ReScript Advent of code

## Installation

```sh
npm install
```

## Build

- Build: `npm run build` / `yarn build`
- Clean: `npm run clean` / `yarn clean`
- Build & watch: `npm run start` `yarn start`

## Run

```sh
node src/Day1.bs.js
```